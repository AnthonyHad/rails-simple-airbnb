<div class="container">
  <h1>Edit the Flat!</h1>

  <%= simple_form_for(@flat) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :description, as: :text %>
  <%= f.input :price_per_night, as: :integer %>
  <%= f.input :number_of_guests, as: :integer %>
  <%= f.button :submit, "Edit now!" %>
  <% end %>
</div>
